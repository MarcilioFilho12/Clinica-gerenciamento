[build]
builder = "NIXPACKS"

[deploy]
# Root Directory no painel Railway = Back-end-clinica
startCommand = "php artisan marag:doctor --fix && php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheckPath = "/up"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
